# 数据库迁移指南：本地 → 服务器

## 📋 前置准备

### 1. 确认数据库信息
- **本地数据库名称**：`supermarket_db`（根据你的实际情况调整）
- **服务器IP**：`8.136.43.180`
- **MySQL端口**：`3306`（默认）

### 2. 确认服务器MySQL已安装并运行
```bash
# 在服务器上检查MySQL状态
systemctl status mysql
# 或
systemctl status mysqld
```

---

## 🔽 步骤1：导出本地数据库

### ⚠️ 重要：编码问题解决方案

**常见错误**：`ERROR: ASCII '\0' appeared in the statement` 或 `Query: 'ÿþ-'`

**原因**：SQL文件编码不正确（可能是UTF-16），MySQL需要UTF-8编码

**解决方案**：使用正确的导出命令，确保输出UTF-8编码

### Windows系统（使用命令行）

打开 **命令提示符（CMD）** 或 **PowerShell**，执行以下命令：

```bash
# ⭐ 推荐方式：使用UTF-8编码导出（避免编码问题）
chcp 65001
mysqldump -u root -p --default-character-set=utf8mb4 --set-charset --databases supermarket_db > supermarket_db_backup.sql

# 或者使用完整路径（如果mysqldump不在PATH中）
chcp 65001
"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqldump.exe" -u root -p --default-character-set=utf8mb4 --set-charset --databases supermarket_db > supermarket_db_backup.sql

# 方式2：导出数据库结构和数据（指定字符集，避免中文乱码）
mysqldump -u root -p --default-character-set=utf8mb4 --set-charset supermarket_db > supermarket_db_backup.sql

# 方式3：导出所有数据库（如果supermarket_db不存在，可能是其他名称）
mysqldump -u root -p --default-character-set=utf8mb4 --set-charset --all-databases > all_databases_backup.sql
```

**关键参数说明**：
- `chcp 65001`：设置Windows命令行编码为UTF-8
- `--default-character-set=utf8mb4`：指定字符集
- `--set-charset`：在SQL文件中添加 `SET NAMES utf8mb4` 语句
- `--databases`：包含CREATE DATABASE语句

**说明**：
- `-u root`：MySQL用户名（根据你的实际情况修改）
- `-p`：会提示输入密码
- `supermarket_db`：数据库名称（根据你的实际情况修改）
- `> supermarket_db_backup.sql`：导出文件名

### 如果提示找不到 mysqldump 命令

**方法1：使用完整路径**
```bash
# MySQL 8.0 默认安装路径
"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqldump.exe" -u root -p supermarket_db > supermarket_db_backup.sql
```

**方法2：添加到系统环境变量**
1. 右键"此电脑" → "属性" → "高级系统设置"
2. 点击"环境变量"
3. 在"系统变量"中找到 `Path`，点击"编辑"
4. 添加MySQL bin目录：`C:\Program Files\MySQL\MySQL Server 8.0\bin`
5. 重启命令行窗口

### 使用MySQL Workbench（图形化工具）

1. 打开 **MySQL Workbench**
2. 连接到本地数据库
3. 点击菜单：**Server** → **Data Export**
4. 选择 `supermarket_db` 数据库
5. 选择要导出的表（或全选）
6. 选择导出选项：
   - ✅ Export to Self-Contained File
   - ✅ Include Create Schema
7. 点击 **Start Export**
8. 保存文件为 `supermarket_db_backup.sql`

---

## 📤 步骤2：上传文件到服务器

### 方法1：使用SCP命令（推荐）

在 **PowerShell** 或 **CMD** 中执行：

```bash
# 上传文件到服务器
scp supermarket_db_backup.sql root@8.136.43.180:/root/

# 如果需要指定SSH端口（默认22）
scp -P 22 supermarket_db_backup.sql root@8.136.43.180:/root/
```

**说明**：
- `root`：服务器用户名（根据实际情况修改）
- `8.136.43.180`：服务器IP
- `/root/`：服务器上的目标目录（根据实际情况修改）

### 方法2：使用WinSCP（图形化工具）

1. 下载并安装 [WinSCP](https://winscp.net/)
2. 打开WinSCP，新建连接：
   - **主机名**：`8.136.43.180`
   - **用户名**：`root`（或你的服务器用户名）
   - **密码**：你的服务器密码
   - **端口**：`22`（SSH默认端口）
3. 点击"登录"
4. 左侧选择本地文件 `supermarket_db_backup.sql`
5. 右侧选择服务器目录（如 `/root/`）
6. 拖拽文件或点击"上传"

### 方法3：使用Xshell + Xftp

1. 使用 **Xshell** 连接服务器
2. 打开 **Xftp**，连接到同一服务器
3. 左侧选择本地文件，右侧选择服务器目录
4. 拖拽上传

---

## 📥 步骤3：在服务器上导入数据库

### 方式1：SSH登录服务器后导入

```bash
# 1. SSH登录服务器
ssh root@8.136.43.180

# 2. 登录MySQL
mysql -u root -p

# 3. 创建数据库（如果不存在）
CREATE DATABASE IF NOT EXISTS supermarket_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 4. 退出MySQL
exit;

# 5. 导入数据库
mysql -u root -p supermarket_db < /root/supermarket_db_backup.sql
```

### 方式2：一条命令完成（推荐）

```bash
# SSH登录后，直接执行
mysql -u root -p supermarket_db < /root/supermarket_db_backup.sql
```

### 方式3：如果SQL文件包含CREATE DATABASE语句

```bash
# 直接导入，会自动创建数据库
mysql -u root -p < /root/supermarket_db_backup.sql
```

### 方式4：在MySQL命令行中导入

```bash
# 1. 登录MySQL
mysql -u root -p

# 2. 创建数据库
CREATE DATABASE IF NOT EXISTS supermarket_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 3. 使用数据库
USE supermarket_db;

# 4. 设置字符集
SET NAMES utf8mb4;

# 5. 导入SQL文件
SOURCE /root/supermarket_db_backup.sql;

# 6. 验证导入
SHOW TABLES;
SELECT COUNT(*) FROM sys_user;  # 检查数据是否导入成功
```

---

## ✅ 步骤4：验证导入结果

在服务器MySQL中执行：

```sql
-- 查看所有表
SHOW TABLES;

-- 查看数据库字符集
SHOW CREATE DATABASE supermarket_db;

-- 检查数据量
SELECT COUNT(*) FROM sys_user;
SELECT COUNT(*) FROM product;
SELECT COUNT(*) FROM purchase_order;
SELECT COUNT(*) FROM sale_order;
SELECT COUNT(*) FROM inventory;

-- 查看表结构
DESC sys_user;
```

---

## 🔧 常见问题解决

### ⚠️ 问题0：导入时出现 `ERROR: ASCII '\0' appeared` 或 `Query: 'ÿþ-'`

**错误原因**：
- SQL文件编码不正确（可能是UTF-16或包含BOM标记）
- Windows导出的文件可能使用了错误的编码
- 文件包含二进制数据或特殊字符

**解决方案1：重新导出（推荐）**

在Windows上使用正确的命令重新导出：
```bash
# 设置UTF-8编码
chcp 65001

# 使用正确的参数导出
mysqldump -u root -p --default-character-set=utf8mb4 --set-charset --databases supermarket_db > supermarket_db_backup.sql
```

**解决方案2：在服务器上转换文件编码**

如果已经上传了错误的文件，可以在服务器上转换：
```bash
# 方法1：使用iconv转换编码（推荐）
iconv -f UTF-16LE -t UTF-8 /tmp/supermarket_db_backup2.sql > /tmp/supermarket_db_backup_utf8.sql

# 方法2：如果文件是UTF-16 BE
iconv -f UTF-16BE -t UTF-8 /tmp/supermarket_db_backup2.sql > /tmp/supermarket_db_backup_utf8.sql

# 方法3：自动检测编码并转换
file /tmp/supermarket_db_backup2.sql  # 先查看文件编码
iconv -f $(file -bi /tmp/supermarket_db_backup2.sql | cut -d= -f2) -t UTF-8 /tmp/supermarket_db_backup2.sql > /tmp/supermarket_db_backup_utf8.sql

# 然后导入转换后的文件
mysql -u root -p supermarket < /tmp/supermarket_db_backup_utf8.sql
```

**解决方案3：使用dos2unix转换（如果文件有Windows换行符）**

```bash
# 安装dos2unix（如果没有）
yum install dos2unix -y  # CentOS/RHEL
# 或
apt-get install dos2unix -y  # Ubuntu/Debian

# 转换文件
dos2unix /tmp/supermarket_db_backup2.sql
mysql -u root -p supermarket < /tmp/supermarket_db_backup2.sql
```

**解决方案4：使用--binary-mode（临时方案，不推荐）**

```bash
mysql -u root -p --binary-mode supermarket < /tmp/supermarket_db_backup2.sql
```

**检查文件编码的方法**：
```bash
# 在服务器上检查文件编码
file /tmp/supermarket_db_backup2.sql
file -i /tmp/supermarket_db_backup2.sql

# 查看文件前几个字节（检查BOM）
head -c 10 /tmp/supermarket_db_backup2.sql | od -An -tx1
# UTF-8 BOM: ef bb bf
# UTF-16 LE BOM: ff fe (这就是你看到的ÿþ)
# UTF-16 BE BOM: fe ff
```

**最佳实践**：
1. 导出时使用 `chcp 65001` 设置UTF-8编码
2. 使用 `--default-character-set=utf8mb4 --set-charset` 参数
3. 上传前检查文件编码
4. 如果出错，在服务器上转换编码后再导入

---

### 问题1：导出时提示"Access denied"

**解决方案**：
```bash
# 检查MySQL用户权限
mysql -u root -p
GRANT ALL PRIVILEGES ON *.* TO 'root'@'localhost';
FLUSH PRIVILEGES;
```

### 问题2：导入时出现中文乱码

**解决方案**：
```bash
# 导出时指定字符集
mysqldump -u root -p --default-character-set=utf8mb4 supermarket_db > supermarket_db_backup.sql

# 导入时指定字符集
mysql -u root -p --default-character-set=utf8mb4 supermarket_db < supermarket_db_backup.sql
```

### 问题3：导入时提示"Table already exists"

**解决方案**：
```bash
# 先删除数据库（谨慎操作！）
mysql -u root -p
DROP DATABASE IF EXISTS supermarket_db;
CREATE DATABASE supermarket_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;

# 重新导入
mysql -u root -p supermarket_db < /root/supermarket_db_backup.sql
```

### 问题4：SCP上传失败（连接被拒绝）

**解决方案**：
1. 检查服务器SSH服务是否开启：`systemctl status sshd`
2. 检查防火墙是否开放22端口
3. 确认服务器IP和用户名正确

### 问题5：导入时提示"Unknown database"

**解决方案**：
```bash
# 先创建数据库
mysql -u root -p
CREATE DATABASE supermarket_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;
```

---

## 📝 完整操作示例（Windows PowerShell）

```powershell
# ========== 步骤1：导出本地数据库 ==========
# 打开PowerShell，切换到项目目录
cd D:\code\supermarket-system

# 导出数据库（会提示输入MySQL密码）
mysqldump -u root -p --default-character-set=utf8mb4 --databases supermarket_db > supermarket_db_backup_$(Get-Date -Format "yyyyMMdd_HHmmss").sql

# ========== 步骤2：上传到服务器 ==========
# 上传文件（会提示输入服务器密码）
scp supermarket_db_backup_*.sql root@8.136.43.180:/root/

# ========== 步骤3：SSH登录服务器并导入 ==========
# SSH登录（会提示输入服务器密码）
ssh root@8.136.43.180

# 在服务器上执行（会提示输入MySQL密码）
mysql -u root -p < /root/supermarket_db_backup_*.sql

# 或者先创建数据库再导入
mysql -u root -p
CREATE DATABASE IF NOT EXISTS supermarket_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;
mysql -u root -p supermarket_db < /root/supermarket_db_backup_*.sql
```

---

## 🎯 快速命令总结

### 本地导出
```bash
mysqldump -u root -p --default-character-set=utf8mb4 --databases supermarket_db > backup.sql
```

### 上传到服务器
```bash
scp backup.sql root@8.136.43.180:/root/
```

### 服务器导入
```bash
ssh root@8.136.43.180
mysql -u root -p < /root/backup.sql
```

---

## ⚠️ 注意事项

1. **备份现有数据**：导入前建议先备份服务器上的现有数据库
2. **字符集设置**：确保使用 `utf8mb4` 字符集，避免中文乱码
3. **权限检查**：确保MySQL用户有创建数据库和表的权限
4. **文件大小**：如果数据库很大，上传可能需要较长时间
5. **网络稳定**：上传和导入过程中保持网络连接稳定
6. **密码安全**：不要在命令中直接写密码，使用 `-p` 参数交互式输入

---

## 📞 需要帮助？

如果遇到问题，请提供：
1. 错误信息截图
2. 执行的命令
3. MySQL版本信息：`mysql --version`
4. 操作系统版本

