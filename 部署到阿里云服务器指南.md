# 超市库存管理系统 - 阿里云服务器部署指南

## 📋 部署概览

本次更新包括：
1. ✅ 修复库存分页问题
2. ✅ 修复管理员库存权限问题
3. ✅ 生成5个月真实数据

需要更新的内容：
- **前端**: 重新打包 `dist` 目录
- **后端**: 重新打包 `jar` 文件
- **数据库**: 导入新的数据

## 🚀 部署步骤

### 第零步：修改配置文件（重要！）

**在打包前，必须先修改配置文件，将 localhost 改为服务器IP**

#### 0.1 修改后端配置

编辑 `supermarket-backend/src/main/resources/application.yml`：

```yaml
# 数据库连接（第6行）
datasource:
  url: jdbc:mysql://8.136.43.180:3306/supermarket_db?...
  # url: jdbc:mysql://localhost:3306/supermarket_db?...  # 注释掉本地配置

# Redis连接（第16行）
redis:
  host: 8.136.43.180
  # host: localhost  # 注释掉本地配置
```

**✅ 已修改完成**，可以直接打包！

#### 0.2 前端配置说明

前端使用相对路径 `/api`，由Nginx代理，**无需修改**。

详细说明请查看 `配置修改说明.md` 文件。

---

### 第一步：本地打包

#### 1.1 打包前端

```bash
# 进入前端目录
cd d:\code\supermarket-system\supermarket-frontend

# 安装依赖（如果之前没安装）
npm install

# 打包生产环境代码
npm run build
```

**打包完成后**：
- 会在 `supermarket-frontend` 目录下生成 `dist` 文件夹
- 这个文件夹包含所有前端静态资源

#### 1.2 打包后端

```bash
# 进入后端目录
cd d:\code\supermarket-system\supermarket-backend

# 使用 Maven 打包（跳过测试）
mvn clean package -DskipTests
```

**打包完成后**：
- JAR 文件位置：`supermarket-backend/target/supermarket-backend-0.0.1-SNAPSHOT.jar`
- 或者类似名称的 jar 文件

**如果没有 Maven 命令**，可以使用 IDEA：
1. 打开 IDEA
2. 右侧点击 Maven 面板
3. 展开 `supermarket-backend` -> `Lifecycle`
4. 双击 `clean`，然后双击 `package`

#### 1.3 准备数据库文件

```bash
# 数据库文件已经生成好了
d:\code\supermarket-system\sql\supermarket_真实数据_完整版.sql
```

### 第二步：上传文件到服务器

#### 2.1 连接服务器

使用以下任一工具连接到阿里云服务器：
- **Xshell** + **Xftp**（推荐）
- **MobaXterm**（推荐，集成SSH和SFTP）
- **WinSCP** + **PuTTY**
- **VS Code Remote SSH**

**连接信息**：
- 服务器IP: `8.136.43.180`
- 用户名: `root`
- 密码: 你的服务器密码
- 端口: `22`

#### 2.2 上传前端文件

**方式一：使用 SFTP 工具（推荐）**

1. 使用 Xftp 或 WinSCP 连接服务器
2. 导航到服务器的 Nginx 静态文件目录（通常是）：
   ```
   /usr/share/nginx/html/
   或
   /var/www/html/
   或
   /www/wwwroot/你的域名/
   ```
3. 备份旧的 `dist` 文件夹：
   ```bash
   mv dist dist_backup_20251210
   ```
4. 上传本地的 `dist` 文件夹到服务器

**方式二：使用命令行（在本地 PowerShell）**

```powershell
# 压缩 dist 文件夹
Compress-Archive -Path "d:\code\supermarket-system\supermarket-frontend\dist\*" -DestinationPath "dist.zip"

# 使用 scp 上传（需要安装 OpenSSH）
scp dist.zip root@8.136.43.180:/tmp/

# 然后 SSH 登录服务器解压
ssh root@8.136.43.180
cd /usr/share/nginx/html/
mv dist dist_backup_20251210
unzip /tmp/dist.zip -d dist
```

#### 2.3 上传后端 JAR 文件

1. 找到打包好的 JAR 文件：
   ```
   d:\code\supermarket-system\supermarket-backend\target\supermarket-backend-0.0.1-SNAPSHOT.jar
   ```

2. 使用 SFTP 上传到服务器（通常放在）：
   ```
   /root/supermarket/
   或
   /opt/supermarket/
   或
   /home/supermarket/
   ```

3. 备份旧的 JAR 文件：
   ```bash
   mv supermarket-backend-0.0.1-SNAPSHOT.jar supermarket-backend-backup-20251210.jar
   ```

#### 2.4 上传数据库文件

使用 SFTP 上传 SQL 文件到服务器：
```
本地: d:\code\supermarket-system\sql\supermarket_真实数据_完整版.sql
服务器: /tmp/supermarket_data.sql
```

### 第三步：服务器端操作

#### 3.1 更新前端（在服务器上执行）

```bash
# SSH 连接到服务器
ssh root@8.136.43.180

# 检查 Nginx 配置
nginx -t

# 如果配置正确，重新加载 Nginx
nginx -s reload

# 或者重启 Nginx
systemctl restart nginx

# 验证前端文件
ls -lh /usr/share/nginx/html/dist/
```

#### 3.2 更新后端（在服务器上执行）

```bash
# 1. 停止旧的后端服务
# 查找 Java 进程
ps aux | grep supermarket

# 停止进程（假设 PID 是 12345）
kill -9 12345

# 或者如果使用 systemd 管理
systemctl stop supermarket

# 2. 进入后端目录
cd /root/supermarket/  # 根据你的实际路径调整

# 3. 备份旧的 JAR（如果还没备份）
mv supermarket-backend-0.0.1-SNAPSHOT.jar supermarket-backend-backup-20251210.jar

# 4. 启动新的后端服务
nohup java -jar supermarket-backend-0.0.1-SNAPSHOT.jar > app.log 2>&1 &

# 5. 查看日志确认启动成功
tail -f app.log

# 按 Ctrl+C 退出日志查看

# 6. 验证后端是否启动
ps aux | grep supermarket
netstat -tlnp | grep 8080
```

**如果使用 systemd 管理服务**：

```bash
# 重启服务
systemctl restart supermarket

# 查看状态
systemctl status supermarket

# 查看日志
journalctl -u supermarket -f
```

#### 3.3 更新数据库（在服务器上执行）

```bash
# 1. 备份现有数据库（重要！）
mysqldump -uroot -p supermarket_db > /tmp/supermarket_db_backup_20251210.sql

# 2. 导入新数据
mysql -uroot -p supermarket_db < /tmp/supermarket_data.sql

# 输入密码后等待导入完成

# 3. 验证数据
mysql -uroot -p
```

在 MySQL 中执行：
```sql
USE supermarket_db;

-- 查看数据统计
SELECT '商品' as 表名, COUNT(*) as 数量 FROM product
UNION ALL
SELECT '库存', COUNT(*) FROM inventory
UNION ALL
SELECT '采购订单', COUNT(*) FROM purchase_order
UNION ALL
SELECT '销售订单', COUNT(*) FROM sale_order;

-- 验证库存公式
SELECT 
    p.product_name,
    IFNULL(purchase.qty, 0) as 采购量,
    IFNULL(sale.qty, 0) as 销售量,
    i.quantity as 当前库存,
    (IFNULL(purchase.qty, 0) - IFNULL(sale.qty, 0)) as 计算库存
FROM product p
LEFT JOIN inventory i ON p.id = i.product_id
LEFT JOIN (
    SELECT product_id, SUM(quantity) as qty
    FROM purchase_order_item
    GROUP BY product_id
) purchase ON p.id = purchase.product_id
LEFT JOIN (
    SELECT product_id, SUM(quantity) as qty
    FROM sale_order_item
    GROUP BY product_id
) sale ON p.id = sale.product_id
LIMIT 10;

EXIT;
```

### 第四步：验证部署

#### 4.1 验证前端

1. 打开浏览器访问：`http://8.136.43.180`
2. 按 `Ctrl + F5` 强制刷新（清除缓存）
3. 检查：
   - ✅ 页面是否正常加载
   - ✅ 样式是否正确
   - ✅ 功能是否正常

#### 4.2 验证后端

1. 检查后端 API：
   ```bash
   curl http://8.136.43.180:8080/api/health
   ```

2. 查看后端日志：
   ```bash
   tail -100 /root/supermarket/app.log
   ```

#### 4.3 验证数据库

1. 登录系统
2. 进入库存管理页面
3. 检查：
   - ✅ 是否显示60条数据
   - ✅ 分页是否正常（6页）
   - ✅ 管理员是否可以入库/出库

#### 4.4 功能测试

- ✅ 登录/登出
- ✅ 库存管理分页
- ✅ 库存调整（入库/出库）
- ✅ 采购订单查看
- ✅ 销售订单查看
- ✅ 数据统计

## 📝 详细命令清单

### 完整部署脚本（在服务器上执行）

```bash
#!/bin/bash
# 超市系统部署脚本

echo "=========================================="
echo "超市库存管理系统 - 部署脚本"
echo "=========================================="

# 1. 备份旧版本
echo "1. 备份旧版本..."
cd /root/supermarket/
cp supermarket-backend-0.0.1-SNAPSHOT.jar supermarket-backend-backup-$(date +%Y%m%d).jar
cd /usr/share/nginx/html/
mv dist dist_backup_$(date +%Y%m%d)

# 2. 备份数据库
echo "2. 备份数据库..."
mysqldump -uroot -proot supermarket_db > /tmp/supermarket_db_backup_$(date +%Y%m%d).sql

# 3. 停止后端服务
echo "3. 停止后端服务..."
ps aux | grep supermarket-backend | grep -v grep | awk '{print $2}' | xargs kill -9

# 4. 解压新的前端文件
echo "4. 部署前端..."
cd /usr/share/nginx/html/
unzip -o /tmp/dist.zip -d dist

# 5. 导入新数据
echo "5. 导入新数据..."
mysql -uroot -proot supermarket_db < /tmp/supermarket_data.sql

# 6. 启动新的后端服务
echo "6. 启动后端服务..."
cd /root/supermarket/
nohup java -jar supermarket-backend-0.0.1-SNAPSHOT.jar > app.log 2>&1 &

# 7. 重启 Nginx
echo "7. 重启 Nginx..."
nginx -s reload

echo "=========================================="
echo "部署完成！"
echo "=========================================="
echo "请访问: http://8.136.43.180"
echo "查看日志: tail -f /root/supermarket/app.log"
```

保存为 `deploy.sh`，然后执行：
```bash
chmod +x deploy.sh
./deploy.sh
```

## ⚠️ 注意事项

### 1. 数据库备份（重要！）

**在导入新数据前，务必备份现有数据库**：
```bash
mysqldump -uroot -p supermarket_db > backup_$(date +%Y%m%d_%H%M%S).sql
```

### 2. 服务器路径

根据你的实际服务器配置调整以下路径：
- Nginx 静态文件目录
- 后端 JAR 文件目录
- 日志文件位置

### 3. 端口检查

确保以下端口开放：
- `80`: HTTP（前端）
- `8080`: 后端 API
- `3306`: MySQL（如果需要远程访问）

### 4. 防火墙配置

```bash
# 检查防火墙状态
firewall-cmd --state

# 开放端口（如果需要）
firewall-cmd --permanent --add-port=80/tcp
firewall-cmd --permanent --add-port=8080/tcp
firewall-cmd --reload
```

### 5. Nginx 配置

确保 Nginx 配置正确：
```nginx
server {
    listen 80;
    server_name 8.136.43.180;
    
    # 前端静态文件
    location / {
        root /usr/share/nginx/html/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    
    # 后端 API 代理
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### 6. 常见问题

**问题1：后端启动失败**
```bash
# 查看日志
tail -100 app.log

# 检查端口占用
netstat -tlnp | grep 8080

# 检查 Java 版本
java -version
```

**问题2：前端页面空白**
```bash
# 检查 Nginx 配置
nginx -t

# 查看 Nginx 错误日志
tail -50 /var/log/nginx/error.log

# 检查文件权限
ls -lh /usr/share/nginx/html/dist/
```

**问题3：数据库连接失败**
```bash
# 检查 MySQL 状态
systemctl status mysql

# 测试连接
mysql -uroot -p -e "SELECT 1"

# 检查后端配置文件中的数据库连接信息
```

## 🔄 回滚方案

如果部署出现问题，可以快速回滚：

### 回滚前端
```bash
cd /usr/share/nginx/html/
rm -rf dist
mv dist_backup_20251210 dist
nginx -s reload
```

### 回滚后端
```bash
cd /root/supermarket/
kill -9 $(ps aux | grep supermarket-backend | grep -v grep | awk '{print $2}')
mv supermarket-backend-backup-20251210.jar supermarket-backend-0.0.1-SNAPSHOT.jar
nohup java -jar supermarket-backend-0.0.1-SNAPSHOT.jar > app.log 2>&1 &
```

### 回滚数据库
```bash
mysql -uroot -p supermarket_db < /tmp/supermarket_db_backup_20251210.sql
```

## 📞 技术支持

如果遇到问题：
1. 查看日志文件
2. 检查服务状态
3. 验证配置文件
4. 测试网络连接

## 📅 部署检查清单

- [ ] 本地前端打包完成
- [ ] 本地后端打包完成
- [ ] 数据库文件准备完成
- [ ] 连接到服务器成功
- [ ] 备份旧的前端文件
- [ ] 备份旧的后端 JAR
- [ ] 备份现有数据库
- [ ] 上传新的前端文件
- [ ] 上传新的后端 JAR
- [ ] 上传数据库文件
- [ ] 停止旧的后端服务
- [ ] 导入新的数据库
- [ ] 启动新的后端服务
- [ ] 重启 Nginx
- [ ] 验证前端访问
- [ ] 验证后端 API
- [ ] 验证数据库数据
- [ ] 功能测试通过

---

**祝部署顺利！** 🎉
