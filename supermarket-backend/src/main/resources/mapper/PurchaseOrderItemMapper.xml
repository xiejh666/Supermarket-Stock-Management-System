<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supermarket.mapper.PurchaseOrderItemMapper">

    <resultMap id="PurchaseOrderItemVOMap" type="com.supermarket.vo.PurchaseOrderItemVO">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_code" property="productCode"/>
        <result column="quantity" property="quantity"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="total_price" property="totalPrice"/>
    </resultMap>

    <select id="selectItemVOListByOrderId" resultMap="PurchaseOrderItemVOMap">
        SELECT
            poi.id,
            poi.order_id,
            poi.product_id,
            p.product_name,
            p.product_code,
            poi.quantity,
            poi.unit_price,
            poi.total_price
        FROM purchase_order_item poi
        LEFT JOIN product p ON poi.product_id = p.id
        WHERE poi.order_id = #{orderId}
    </select>

</mapper>



